name: CI

on:
  - push
  - pull_request

permissions:
  contents: read

env:
  # Example DID identity (in production, set this in repo/org variables instead of committing)
  DID_IDENTITY: "did:ion:EiD8J2b3K8k9Q8x9L7m2n4p1q5r6s7t8u9v0w1x2y3z4A5B6C7D8E9F0"

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: CI smoke test
        run: |
          echo "GitHub-Faraday CI smoke test"
          echo "Using DID identity: $DID_IDENTITY"